<Window x:Class="QLKS_AnPhu.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Quản lý khách sạn"
        Width="1440"
        Height="860"
        MinWidth="1180"
        MinHeight="720"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource AppBackgroundBrush}">
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="SidebarColumn" Width="292" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border x:Name="SidebarShell"
                Grid.Column="0"
                Margin="16"
                CornerRadius="25"
                Background="{StaticResource SidebarBrush}"
                BorderBrush="#E2E8F0"
                BorderThickness="1"
                Effect="{StaticResource StrongShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid x:Name="BrandHeader" Margin="18,20,18,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="48" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border x:Name="BrandLogo" Width="48" Height="48" CornerRadius="16">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#2563EB" Offset="0" />
                                <GradientStop Color="#8B5CF6" Offset="1" />
                            </LinearGradientBrush>
                        </Border.Background>
                        <iconPacks:PackIconMaterial Kind="HomeCity" Width="26" Height="26" Foreground="White" />
                    </Border>

                    <StackPanel x:Name="BrandTextPanel" Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="LblBrandTitle" Text="AN PHÚ HOTEL" Foreground="White" FontSize="18" FontWeight="Bold" />
                        <TextBlock x:Name="LblBrandSubtitle" Text="Management Suite" Foreground="#94A3B8" FontSize="12" />
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="1" Margin="0,6,0,0">
                    <Button x:Name="BtnTrangChu" Style="{StaticResource SidebarButtonActiveStyle}" Click="BtnTrangChu_Click">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Home" Width="20" Height="20" Margin="0,0,12,0" />
                            <TextBlock x:Name="LblTrangChu" Text="Trang chủ" Style="{StaticResource ButtonContentTextStyle}" />
                        </StackPanel>
                    </Button>

                    <Button x:Name="BtnMenuQLPhong" Style="{StaticResource SidebarButtonStyle}" Click="BtnQLPhong_Click">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Bed" Width="20" Height="20" Margin="0,0,12,0" />
                            <TextBlock x:Name="LblQuanLyPhong" Text="Quản lý phòng" Style="{StaticResource ButtonContentTextStyle}" />
                        </StackPanel>
                    </Button>

                    <Button x:Name="BtnMenuKhachHang" Style="{StaticResource SidebarButtonStyle}" Click="BtnKhachHang_Click">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="AccountGroup" Width="20" Height="20" Margin="0,0,12,0" />
                            <TextBlock x:Name="LblKhachHang" Text="Khách hàng" Style="{StaticResource ButtonContentTextStyle}" />
                        </StackPanel>
                    </Button>

                    <Button x:Name="BtnMenuNhanVien" Style="{StaticResource SidebarButtonStyle}" Click="BtnNhanVien_Click">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="AccountTie" Width="20" Height="20" Margin="0,0,12,0" />
                            <TextBlock x:Name="LblNhanVien" Text="Nhân viên" Style="{StaticResource ButtonContentTextStyle}" />
                        </StackPanel>
                    </Button>

                    <Button x:Name="BtnMenuPhieuThue" Style="{StaticResource SidebarButtonStyle}" Click="BtnPhieuThue_Click">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="ClipboardText" Width="20" Height="20" Margin="0,0,12,0" />
                            <TextBlock x:Name="LblPhieuThue" Text="Phiếu thuê" Style="{StaticResource ButtonContentTextStyle}" />
                        </StackPanel>
                    </Button>

                    <Button x:Name="BtnMenuHoaDon" Style="{StaticResource SidebarButtonStyle}" Click="BtnHoaDon_Click">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="FileDocumentMultiple" Width="20" Height="20" Margin="0,0,12,0" />
                            <TextBlock x:Name="LblHoaDon" Text="Hóa đơn" Style="{StaticResource ButtonContentTextStyle}" />
                        </StackPanel>
                    </Button>

                    <Button x:Name="BtnMenuDichVuVatTu" Style="{StaticResource SidebarButtonStyle}" Click="BtnDichVuVatTu_Click">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="PackageVariantClosed" Width="20" Height="20" Margin="0,0,12,0" />
                            <TextBlock x:Name="LblDichVu" Text="Dịch vụ" Style="{StaticResource ButtonContentTextStyle}" />
                        </StackPanel>
                    </Button>

                    <Button x:Name="BtnMenuVatTu" Style="{StaticResource SidebarButtonStyle}" Click="BtnVatTu_Click">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="PackageVariantClosed" Width="20" Height="20" Margin="0,0,12,0" />
                            <TextBlock x:Name="LblVatTu" Text="Vật tư" Style="{StaticResource ButtonContentTextStyle}" />
                        </StackPanel>
                    </Button>

                    <Button x:Name="BtnMenuBaoCao" Style="{StaticResource SidebarButtonStyle}" Click="BtnBaoCao_Click">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="FileChart" Width="20" Height="20" Margin="0,0,12,0" />
                            <TextBlock x:Name="LblBaoCao" Text="Báo cáo" Style="{StaticResource ButtonContentTextStyle}" />
                        </StackPanel>
                    </Button>

                    <Button x:Name="BtnMenuCaiDat" Style="{StaticResource SidebarButtonStyle}" Click="BtnCaiDat_Click">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Cog" Width="20" Height="20" Margin="0,0,12,0" />
                            <TextBlock x:Name="LblCaiDat" Text="Cài đặt" Style="{StaticResource ButtonContentTextStyle}" />
                        </StackPanel>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,0,0,18">
                    <Border x:Name="SidebarDivider" Margin="18,8,18,12" Height="1" Background="#E2E8F0" />
                    <Button x:Name="BtnSidebarLogout" Style="{StaticResource SidebarButtonStyle}" Click="BtnDangXuat_Click">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Logout" Width="20" Height="20" Margin="0,0,12,0" />
                            <TextBlock x:Name="LblDangXuat" Text="Đăng xuất" Style="{StaticResource ButtonContentTextStyle}" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Margin="0,16,16,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="74" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource HeaderBrush}" CornerRadius="18" BorderBrush="#E5EAF2" BorderThickness="1" Effect="{StaticResource SoftShadow}">
                <Grid Margin="18,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Button Width="44" Height="44" VerticalAlignment="Center" Style="{StaticResource AppOutlineButtonStyle}" Padding="0" Click="BtnToggleMenu_Click">
                        <iconPacks:PackIconMaterial Kind="Menu" Width="20" Height="20" />
                    </Button>

                    <StackPanel Grid.Column="1" Margin="18,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="TxtPageTitle" Text="Dashboard khách sạn" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextMainBrush}" />
                        <TextBlock Text="Fluent dashboard 2025 - dữ liệu vận hành khách sạn" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,3,0,0" />
                    </StackPanel>

                    <Border Grid.Column="2" Background="#F8FAFC" CornerRadius="14" BorderBrush="#E2E8F0" BorderThickness="1" Padding="14,8" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="AccountCircle" Width="22" Height="22" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,8,0" />
                            <TextBlock x:Name="TxtXinChao" Text="Xin chào: Admin" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{StaticResource TextMainBrush}" />
                        </StackPanel>
                    </Border>

                    <Button Grid.Column="4" Style="{StaticResource AppButtonStyle}" Background="{StaticResource WarningBrush}" BorderBrush="{StaticResource WarningBrush}" Click="BtnDangXuat_Click">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Logout" Width="17" Height="17" Margin="0,0,7,0" />
                            <TextBlock Text="Đăng xuất" Style="{StaticResource ButtonContentTextStyle}" />
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <Border Grid.Row="1" Margin="0,16,0,0" Background="Transparent" CornerRadius="18" ClipToBounds="True">
                <ContentControl x:Name="MainContent" />
            </Border>
        </Grid>
    </Grid>
</Window>

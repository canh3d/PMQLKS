<UserControl x:Class="QLKS_AnPhu.View.QLPhong"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="560"
             d:DesignWidth="1024"
             Background="#EEF3F7">
    <UserControl.Resources>
        <SolidColorBrush x:Key="TextMainBrush" Color="#172033" />
        <SolidColorBrush x:Key="TextMutedBrush" Color="#5D6B82" />
        <SolidColorBrush x:Key="TealBrush" Color="#087987" />
        <SolidColorBrush x:Key="GreenBrush" Color="#0D9E4D" />
        <SolidColorBrush x:Key="RedBrush" Color="#C0392B" />
        <SolidColorBrush x:Key="AmberBrush" Color="#B7791F" />

        <DropShadowEffect x:Key="SoftShadow" BlurRadius="10" Direction="270" ShadowDepth="2" Opacity="0.12" />

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#DAE3EE" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Effect" Value="{StaticResource SoftShadow}" />
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="32" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
    </UserControl.Resources>

    <Grid>
    <Grid x:Name="MainContent" Margin="20,20,20,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="65" />
            <RowDefinition Height="35" />
            <RowDefinition Height="*" />
            <RowDefinition Height="45" />
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Sơ đồ phòng" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextMainBrush}" />
            <TextBlock Text="Theo dõi trạng thái phòng theo dữ liệu thật" Margin="0,4,0,0" FontSize="12" Foreground="{StaticResource TextMutedBrush}" />
            <TextBlock x:Name="TxtLoi" Margin="0,4,0,0" FontSize="11" Foreground="{StaticResource RedBrush}" TextWrapping="Wrap" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="15" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="15" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="15" />
                <ColumnDefinition Width="140" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Trạng thái" Margin="0,0,8,0" VerticalAlignment="Center" FontSize="12" />
            <ComboBox x:Name="CboTrangThai" Grid.Column="1" Height="26" SelectedIndex="0">
                <ComboBoxItem Content="Tất cả" />
                <ComboBoxItem Content="Phòng trống" />
                <ComboBoxItem Content="Đang thuê" />
                <ComboBoxItem Content="Đang sửa chữa" />
            </ComboBox>

            <TextBlock Grid.Column="3" Text="Tầng" Margin="0,0,8,0" VerticalAlignment="Center" FontSize="12" />
            <ComboBox x:Name="CboTang" Grid.Column="4" Height="26" SelectedIndex="0" />

            <Grid Grid.Column="6">
                <TextBox x:Name="TxtTimKiem" Height="26" Padding="8,0,28,0" VerticalContentAlignment="Center" FontSize="11" KeyDown="TxtTimKiem_KeyDown" />
                <iconPacks:PackIconMaterial Kind="Calendar" Width="12" Height="12" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#667085" />
            </Grid>

            <Button Grid.Column="8" Style="{StaticResource ActionButtonStyle}" Background="{StaticResource TealBrush}" BorderBrush="{StaticResource TealBrush}" Foreground="White" Click="BtnTim_Click">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Magnify" Width="12" Height="12" Margin="0,0,4,0" />
                    <TextBlock Text="Tìm" />
                </StackPanel>
            </Button>

            <Button Grid.Column="10" Style="{StaticResource ActionButtonStyle}" Background="White" BorderBrush="#D8E0EA" Foreground="{StaticResource TextMainBrush}" Click="BtnDanhSachPhong_Click">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="FormatListBulleted" Width="12" Height="12" Margin="0,0,6,0" />
                    <TextBlock Text="Danh sách phòng" />
                </StackPanel>
            </Button>
        </Grid>

        <Grid Grid.Row="2" Margin="0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.4*" />
                <ColumnDefinition Width="15" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Padding="15">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="PanelPhongTheoTang" />
                </ScrollViewer>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Padding="18">
                <StackPanel>
                    <TextBlock Text="CHI TIẾT PHÒNG" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextMainBrush}" />
                    <Grid Margin="0,12,0,12">
                        <TextBlock x:Name="TxtCTMaPhong" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TealBrush}" VerticalAlignment="Center" />
                        <Border HorizontalAlignment="Right" Background="#E8F7EE" Padding="10,4" CornerRadius="4">
                            <TextBlock x:Name="TxtCTTrangThai" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource GreenBrush}" />
                        </Border>
                    </Grid>
                    <Border Height="1" Background="#EAECF0" Margin="0,0,0,15" />
                    <TextBlock x:Name="TxtCTTang" FontSize="12" Margin="0,0,0,12" Foreground="#344054" />
                    <TextBlock x:Name="TxtCTLoaiPhong" FontSize="12" Margin="0,0,0,12" Foreground="#344054" />
                    <TextBlock x:Name="TxtCTGiaPhong" FontSize="12" Margin="0,0,0,12" Foreground="#344054" />
                    <TextBlock x:Name="TxtCTKhach" FontSize="12" Margin="0,0,0,12" Foreground="#344054" />
                    <TextBlock x:Name="TxtCTGioNhan" FontSize="12" Margin="0,0,0,12" Foreground="#344054" />
                    <TextBlock x:Name="TxtCTGioTra" FontSize="12" Margin="0,0,0,12" Foreground="#344054" />
                    <TextBox x:Name="TxtCTGhiChu" Height="50" Padding="8" FontSize="11" Background="#F4F6F8" BorderBrush="#CDD6E1" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" />
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="3" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="180" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="140" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Style="{StaticResource ActionButtonStyle}" Background="{StaticResource TealBrush}" BorderBrush="{StaticResource TealBrush}" Foreground="White" Click="BtnDatPhong_Click">
                <TextBlock Text="Đặt phòng" />
            </Button>
            <Button Grid.Column="2" Style="{StaticResource ActionButtonStyle}" Background="#F59E0B" BorderBrush="#F59E0B" Foreground="White" Click="BtnDatPhongTheoDoan_Click">
                <TextBlock Text="Đặt phòng theo đoàn" />
            </Button>
            <Button Grid.Column="4" Style="{StaticResource ActionButtonStyle}" Background="White" BorderBrush="#D8E0EA" Foreground="{StaticResource TextMainBrush}" Click="BtnLamMoi_Click">
                <TextBlock Text="Làm mới" />
            </Button>
        </Grid>

        <Grid Grid.Row="4" VerticalAlignment="Bottom" Margin="0,0,0,5">
            <TextBlock Text="Chú thích: Xanh lá: Phòng trống | Đỏ: Đang thuê | Xanh dương: Đã đặt | Vàng: Chưa dọn dẹp | Xám: Đang sửa chữa" FontSize="11" Foreground="#5D6B82" HorizontalAlignment="Left" />
            <TextBlock Text="Trạng thái hệ thống: Hoạt động tốt" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource GreenBrush}" HorizontalAlignment="Right" />
        </Grid>
    </Grid>

    </Grid>
</UserControl>

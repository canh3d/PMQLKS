<Window x:Class="QLKS_AnPhu.View.NhanVienForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Thông tin nhân viên"
        Width="560"
        Height="620"
        MinWidth="520"
        MinHeight="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F7FB"
        FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2556D9" />
        <Style x:Key="FormTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="42" />
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="materialDesign:TextFieldAssist.HasOutlinedTextField" Value="True" />
        </Style>
        <Style x:Key="FormComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="42" />
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="materialDesign:TextFieldAssist.HasOutlinedTextField" Value="True" />
        </Style>
        <Style x:Key="FormButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="42" />
            <Setter Property="MinWidth" Value="116" />
            <Setter Property="Padding" Value="18,0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="92" />
            <RowDefinition Height="*" />
            <RowDefinition Height="78" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#163C9B" Offset="0" />
                    <GradientStop Color="#2563EB" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel Margin="24,0" VerticalAlignment="Center">
                <TextBlock x:Name="TxtTieuDe" Text="THÊM NHÂN VIÊN" FontSize="24" FontWeight="Bold" Foreground="White" />
                <TextBlock Text="Nhập thông tin nhân viên khách sạn" Margin="0,6,0,0" FontSize="14" Foreground="#DBEAFE" />
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Border Margin="22" Padding="22" Background="White" CornerRadius="16" BorderBrush="#E5E7EB" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="14" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.ColumnSpan="3">
                        <TextBlock Text="Họ tên *" FontWeight="SemiBold" Margin="0,0,0,5" />
                        <TextBox x:Name="TxtHoTen" Style="{StaticResource FormTextBoxStyle}" materialDesign:HintAssist.Hint="Ví dụ: Nguyễn Văn A" />
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0">
                        <TextBlock Text="Giới tính" FontWeight="SemiBold" Margin="0,0,0,5" />
                        <ComboBox x:Name="CboGioiTinh" Style="{StaticResource FormComboBoxStyle}">
                            <ComboBoxItem Content="Nam" />
                            <ComboBoxItem Content="Nữ" />
                            <ComboBoxItem Content="Khác" />
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="2">
                        <TextBlock Text="Ngày sinh" FontWeight="SemiBold" Margin="0,0,0,5" />
                        <DatePicker x:Name="DpNgaySinh"
                                    Height="42"
                                    Margin="0,0,0,12"
                                    materialDesign:TextFieldAssist.HasOutlinedTextField="True"
                                    materialDesign:HintAssist.Hint="Chọn ngày sinh" />
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="0">
                        <TextBlock Text="Số điện thoại" FontWeight="SemiBold" Margin="0,0,0,5" />
                        <TextBox x:Name="TxtSDT" Style="{StaticResource FormTextBoxStyle}" materialDesign:HintAssist.Hint="090..." />
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="2">
                        <TextBlock Text="Chức vụ" FontWeight="SemiBold" Margin="0,0,0,5" />
                        <ComboBox x:Name="CboChucVu" Style="{StaticResource FormComboBoxStyle}" IsEditable="True">
                            <ComboBoxItem Content="Quản lý" />
                            <ComboBoxItem Content="Lễ tân" />
                            <ComboBoxItem Content="Buồng phòng" />
                            <ComboBoxItem Content="Kế toán" />
                            <ComboBoxItem Content="Bảo vệ" />
                            <ComboBoxItem Content="Nhân viên" />
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Grid.ColumnSpan="3">
                        <TextBlock Text="Địa chỉ" FontWeight="SemiBold" Margin="0,0,0,5" />
                        <TextBox x:Name="TxtDiaChi"
                                 Height="82"
                                 Margin="0,0,0,12"
                                 Padding="10"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 materialDesign:TextFieldAssist.HasOutlinedTextField="True"
                                 materialDesign:HintAssist.Hint="Địa chỉ liên hệ" />
                    </StackPanel>

                    <CheckBox x:Name="ChkDangLamViec"
                              Grid.Row="4"
                              Grid.ColumnSpan="3"
                              Content="Đang làm việc"
                              IsChecked="True"
                              FontWeight="SemiBold"
                              Margin="0,4,0,0" />
                </Grid>
            </Border>
        </ScrollViewer>

        <Border Grid.Row="2" Background="White" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="22,14">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource FormButtonStyle}" Background="White" BorderBrush="#CBD5E1" Click="BtnHuy_Click">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Close" Width="16" Height="16" Margin="0,0,6,0" />
                        <TextBlock Text="Hủy" />
                    </StackPanel>
                </Button>
                <Button Margin="10,0,0,0" Style="{StaticResource FormButtonStyle}" Background="{StaticResource PrimaryBrush}" BorderBrush="{StaticResource PrimaryBrush}" Foreground="White" Click="BtnLuu_Click">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="ContentSave" Width="16" Height="16" Margin="0,0,6,0" />
                        <TextBlock Text="Lưu" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
